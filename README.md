# ghostty-themes
My ghostty themes


## Installation
Put theme files into:
```
~/.config/ghostty/themes
```

Then add this to `~/.config/ghostty/config`:
```
theme = [ThemeName]
```

Replace theme name with theme you want to use
